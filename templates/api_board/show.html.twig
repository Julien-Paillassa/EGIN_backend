{% extends 'base.html.twig' %}

{% block title %}Board{% endblock %}

{% block body %}
    <h1>Board</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ board.id }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ board.brand }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ board.name }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ board.size }}</td>
            </tr>
            <tr>
                <th>Volume</th>
                <td>{{ board.volume }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ board.description }}</td>
            </tr>
            <tr>
                <th>Dimension</th>
                <td>{{ board.dimension }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_api_board_index') }}">back to list</a>

    <a href="{{ path('app_api_board_edit', {'id': board.id}) }}">edit</a>

    {{ include('api_board/_delete_form.html.twig') }}
{% endblock %}
